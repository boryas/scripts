# Tests known to reliably hang past the retry-phase per-test timeout
# (currently RETRY_TIMEOUT=30m).  They are NOT run at all -- skipping
# saves ~30 minutes per cron run.  test/run synthesizes a parallel-phase
# timeout entry for them; the digest's verdict logic produces "hang"
# automatically (parallel=timeout + no retry data => hang).
#
# Format: one test per line.  Blank lines and # comments allowed.
#
# Promote to this list ONLY after >= 2 consecutive retry-phase 30m caps.
# Demote (delete) if a kernel update fixes the hang.
btrfs/187
generic/166
