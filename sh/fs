_basic_dev_mnt_usage() {
	if [ $# -lt 2 ]
	then
		echo "usage: $SCRIPT <dev> <mnt>"
		_usage
	fi
}

_cycle_mnt() {
	local dev=$1
	local mnt=$2

	echo "cycle mount $dev $mnt" > /dev/kmsg
	umount $dev
	mount $dev $mnt
}

_umount() {
	local mnt=$1
	set +e
	umount $mnt
	set -e
}


